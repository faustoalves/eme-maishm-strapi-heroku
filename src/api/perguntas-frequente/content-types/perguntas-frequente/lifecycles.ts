// const { sendStatusToRevalidade } = require
module.exports = {
  async afterCreate(event) {
    // sendStatusToRevalidade(SingleTypes.PERGUNTAS_FREQUENTES);
  },
  async afterDelete(event) {
    // sendStatusToRevalidade(SingleTypes.PERGUNTAS_FREQUENTES);
  },
  async afterUpdate(event) {
    // sendStatusToRevalidade(SingleTypes.PERGUNTAS_FREQUENTES);
  },
};
